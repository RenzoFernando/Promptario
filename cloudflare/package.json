{
  "name": "promptario-security-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute DB --remote --file=./schema.sql",
    "db:unlock": "wrangler d1 execute DB --remote --command \"UPDATE security_state SET failed_attempts = 0, locked = 0, locked_at = NULL, lock_event_recorded = 0, updated_at = datetime('now') WHERE id = 1;\""
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
